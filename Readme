This is a small Readme about this.


This is a very specific program I made for a Windows NT 4.0 VM for myself, so it does work only in a very specific way. It works only when your taskbar is on the top of the screen, and when the taskbar is at 2x dimension.
